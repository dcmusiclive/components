import React from 'react';
import {
  Code as NBCode,
  Heading as NBHeading,
  Text as NBText,
} from 'native-base';

export const Code = props => <NBCode {...props} />;

export const Heading = props => <NBHeading {...props} />;

export const Text = props => <NBText {...props} />;
